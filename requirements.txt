# Codex Tier Widget 运行依赖
# tkinter 仍由 Python 自带；pystray + Pillow 用于右下角系统托盘图标。
#
# 安装：
#   python -m pip install -r requirements.txt
#
# 如果运行时报 Python 版本太低（< 3.11），请安装 3.11 或以上版本：
#   https://www.python.org/downloads/

pystray>=0.19.5
Pillow>=10.0
#
# 开发依赖（可选，CI / 本地 lint 用）：
#   pip install -e ".[dev]"   # 安装 ruff / pyflakes
#   或
#   pip install ruff pyflakes
